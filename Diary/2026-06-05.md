倒立摆项目调试/程序编写
	`USE_MOTOR_PID_CONTROL`为0时测试电机正常;
	`USE_MOTOR_PID_CONTROL`为1时测试:
- [x] 需要进入Cubemx把配置的TIM4 CH1 初始PWM值改成0. 防止电机开始时出现起转; ✅ 2026-06-06
- [x] 同时`step_motor_move_angle()`也需要加入PID相关逻辑, 比如设置PID目标速度等. ✅ 2026-06-06
	**如果不加入位置环, 那么是无法PID控制电机经过多少角度的.**
- [ ] 目前的设置电机速度, 由于PID模块的相关参数为0, 导致涉及到比较大的频率(超出启动频率时, 电机无法转动, 并且实际输出也为0, 因为PID参数为0的缘故)
- [x] 电机初始化时的`PID_init()`中的outputmax和min都是0. ✅ 2026-06-06
	- [ ] 
- [x] 调试时需要注意PID_init后的最大输出参数(转速)值是夺少. ✅ 2026-06-06
	- [x] 63( 按照步进频率为210hz计算). ✅ 2026-06-06
- [ ] 检查`step_motor_set_speed()`函数的PID部分内容.
	- [x] 当设置`USE_MOTOR_PID_CONTROL`时, 函数中没有开启PWM导致电机不转动. ✅ 2026-06-06
- [x] 进入TIM溢出中断回调函数时, 发现当时TIM3来的时候会导致注册的算法表中, motor找不到. ✅ 2026-06-06
	- [x] 尝试直接加入TIM3的注册, 自动调参时发现会导致一直运行`step_motor_set_speed()`函数 ✅ 2026-06-06
- [x] 编码器获取到的速度与计划设置的速度是相反的 ✅ 2026-06-06
	- [x] 修改了编码器中断获取到的速度. 取反 ✅ 2026-06-06

- [x] 设计PID串口发送规则 ✅ 2026-06-09
- [ ] 电机驱动库的集成
	- [ ] freertos版本
		- [ ] 明确任务名称和优先级
		- [ ] 完成相关cubemx配置
		- [ ] 完成飞书文档补全
		- [ ] 
	- [ ]  
- [ ] 更新飞书内容
	- [x] 电机驱动使用 ✅ 2026-06-06
		- [x] 接线, ✅ 2026-06-06
		- [x] 软件函数, ✅ 2026-06-06
		- [x] 配置内容. ✅ 2026-06-06

```
=== Motor CMD Test ===
=== Motor Control Commands ===
S:<rpm>,<dir> Set speed (dir: 1=FWD, 2=REV)
A:<angle>,<dir> Move angle (degree)
P Stop motor
Q Query status
M:<mode> Set step mode (1/2/4/8/16)
T Start auto-tune (relay method)
R Query auto-tune result
X:<target>,<kp>,<ki>,<kd> Set PID params + target
H Show this help
==============================
```

- [ ] PID调参
	- [x] 磁编码器的角度获取有误差, 磁铁和电机轴没有固定好, 导致高速转动下有误差. ✅ 2026-06-09
		- [x] 尝试使用502固定, 无法粘牢, 并且有风险 ✅ 2026-06-09
		- [x] 直接设置电机运行指定速度, 查看实际速度的误差.(实测在10rpm左右) ✅ 2026-06-09
		- [ ] 目前的`step_motor_set_speed() & 
	- [x] 串口发送S:500,1, out为什么却是964, 导致电机转不了, S:300,1\n, 是, out=330左右, 什么原因? ✅ 2026-06-09
		- [x] 根因：步进电机失步后形成"PID 死锁"控制链路中存在一个正反馈死锁：电机失步 → 编码器读 0 → PID 输出推到最大(420RPM/1400Hz) → 1400Hz 下失步电机无法重启 → 编码器一直读 0 → 循环...由于 rate limiter 限制为 +20 RPM/cycle (2ms)，PID 输出从起步值 318 涨到饱和值 420 仅需 10ms。即使发送新速度命令重置了 PID，电机在低频起步频率下只停留了 10ms 就被拉回高频，根本没有时间重新建立同步。 ✅ 2026-06-09
	- [ ] 自动调参(本次不适用)
		- [ ] 发送命令后电机匀速运动, 同时不会输出当前的target 和 actual.
	- [ ] 尝试改变TIM的频率可以实现更精细的PID控制.但是实测发现当前电机速度最快只能到达400rpm.
- [ ] 设置指定角度还未测试
- [ ] 


experiment 分支主要更改
	1. 对于freertos的支持
		a. 进入临界区统一定义为使用`CRITICAL_ENTER() OR CRITICAL_EXIT()`(条件编译);
			隐身问题1: 为什么在freertos中使用__disable_irq()会导致调度器被破坏;
		b. 
	2. 加入限位开关支持. 初始化了两个GPIO引脚为浮空输入模式, 当没有限位是默认为GND, 限位开关开:3.3V.
	3. PID调参支持
		a. 增加自动调参功能.
		b. PID增加软重启功能, 避免失步状态下使控制器崩溃;
	4. 增加串口命令解析器
		a. 支持对于系统电机的速度设置, 角度设置, PID调参, 查看命令帮助, 自动调参模式, 获取调参结果. 