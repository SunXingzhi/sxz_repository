- [ ] freertos 源码阅读
	- [ ] 为什么在创建任务时, 需要伪造一个栈帧?
		在创建任务前, 会为一个任务分配一个特定的栈大小(TCB块本身的大小以及任务栈的大小,即传入`xTaskCreate()`的栈深度, 单位为`StackType_t`), 设置TCB相关数据, 比如任务名称, 栈顶位置, 任务优先级, 挂载的节点链表初始化.
		在`pxPortInitialiseStack`中, 
	- [ ] 在搭建这样一个栈帧时这里面每个寄存器存储的信息以及顺序是否是固定死的?
	- [ ] portINITIAL_EXC_RETURN的值是系统自己获取的吗? 他怎么知道是回复主堆栈指针还是线程堆栈指针PSP?