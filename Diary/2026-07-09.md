- [ ] freertos-task源码阅读及调度器实现
	(这里有个问题, 为什么异常时cpu会跳转到这里的定义的函数的入口同时会将数据自动按照指令集回溯,加载到CPU寄存器的位置?)
	- [x] 链表初始化时的pxIndex是否与pxListEnd一致? ✅ 2026-07-10
	- [ ] 从freertos中的宏和函数定义的思考
		- [x] 什么时候设计者会采用宏? 什么时候用函数? ✅ 2026-07-10
			- [x] 移植层. 对于调试, 芯片的寄存器不同, 是否有MPU, 浮点加速单元等, 都是移植需要考虑的范围, 这时需要用到宏来实现移植操作. 比如其中的`FreeRTOSConfig.h`就是通过定义宏来决定freertos的拓展功能. 有很多的调试宏如:`traceENTER_xQueueGiveFromISR`实际没有定义, 需要用户给出定义和实现. ✅ 2026-07-10
			- [x] 处于性能需求高(如freertos实时操作系统的任务切换, 保存现场等等), 此时由于函数调用有栈切换开销, 此时直接用宏更快 ✅ 2026-07-10
			- [x] 访问数据结构字段. 在freertos中, 数据结构都进行了封装抽象, 所以直接操控结构体这种行为是不可取的, 为此定义了每个数据结构(链表)的访问接口宏. ✅ 2026-07-10
		
