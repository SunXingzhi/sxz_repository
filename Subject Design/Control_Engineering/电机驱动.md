提到的 `fastInvSqrt` 是游戏界著名的 **Quake III Arena 快速倒数平方根算法**：

```c
float fastInvSqrt(const float x) {
    const float xhalf = x * 0.5f;
    union { float x; int32_t i; } u;
    u.x = x;
    u.i = 0x5f3759df - (u.i >> 1);  // 魔法数字：牛顿迭代的初始猜测
    return u.x * (1.5f - xhalf * u.x * u.x);  // 一次牛顿迭代精化
}
```
电机指定速度`step_motor_set_speed()`流程:
	1. 获取当前电机速度大小与方向;
	2. 与目标速度大小与方向进行比较:
		1. 如果方向相同则:
			每次增加速度时判断速度差值大小是否是一步长(测量得, 一步长以内, 则直接加到目标速度;一步长以外, 通过加速函数(需要延时5ms, 每次步长是200ms)加速到目标速度
		2. 如果方向相反则:
			1. 将速度先通过减速度曲线减速到0(`step_motor_stop()`, 再通过上面的方向相同的方式执行逻辑

> 非阻塞步进电机加速/减速方法
> 	本质上是通过TIM定时器+中断以及状态机实现的, 可以把电机的运动过程分为几个状态:
> 	1. 空闲状态;
> 	2. 加速状态;
> 	3. 保持速度状态;
> 	4. 减速状态;
> 	5. 完成状态;
> 	计时器(TIM/Systick)根据控制周期(这里设置为5ms一次)来轮询`motor_ramp_t`的状态是否是这个五个里面的某个, 并执行具体相关逻辑.大致的内容如下:
> 	```C
> 	
> 	```

	

电机指定向某个方向运动N个步长流程:
	1. 开启定时器中断. 每次进入中断判断一个信号量, 如果此时是`specified_step`状态, 则需要开始累计一个变量(步长数)
	2. 

电机指定向某个方向运动X角度流程(支持k * 360, k=1, 2, 3, 分辨率最低0.2°16步进):