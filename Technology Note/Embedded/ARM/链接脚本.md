# 链接脚本(ARM)

## 链接脚本的功能

可以知道, 汇编语言中会定义一些数据段, 比如.data, .bss, .text section等等.那么`.s`file 编译后产生的.o文件实际上是不带有内存地址信息的, 而是只带有大小和偏移量, 比如.bss相对于.data起始地址的`offset`是xxx. 在编译后通过链接器(Linkder)将每个`.o`文件根据**链接脚本(*.lds file)**中描述的每个段对应的位置/地址放在一起, 形成了最终的目标文件(`.elf, .bin等`). 也就是说, 没有链接脚本, 就无法生成最终的一个输出文件.

